[project]
name = "alfa243"
version = "0.1.0"
description = "ALFA-243 predictive platform"
requires-python = ">=3.12"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
